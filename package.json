{
  "scripts": {
    "test": "npm run test --prefix server && npm run test --prefix client"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "jsonwebtoken": "^9.0.3",
    "react-router-dom": "^7.14.0"
  }
}
